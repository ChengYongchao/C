#include<stdio.h>

int main(int argc, char* argv[])
{
	//编程题1：读文件file1. txt的内容
	//int res = q1_readFile(0);

	//编程题2：输出和为-一个给定整数的所有组合
	//int res = q2();

	//编程题3：递规反向输出字符串的例子,可谓是反序的经典例程.
	/*======================================解法1=========================================*/
	//char* p = "abcdefg";
	//inverse(p);
	
	/*======================================解法2=========================================*/
	/*FILE* fread = NULL;
	FILE* fwrite = NULL;

	fread = fopen("q3data.txt", "rb");
	fwrite = fopen("q3dataout.txt", "wb");
	int res = inverse2(fread, fwrite);
	fclose(fread);
	fclose(fwrite);
	*/


}